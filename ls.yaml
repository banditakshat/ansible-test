---
  - name: "Playing with Ansible and Git"
    hosts: localhost
    connection: local 
    gather_facts: false
    
    tasks:

    - name: "Execute a command"
      shell: "curl http://44.200.161.215/test.yaml"
      register: "output"

    - debug: var=output.stdout_lines
