---
  - name: "Playing with Ansible and Git"
    hosts: localhost
    connection: local 
    
    tasks:

    - name: "Execute a command"
      shell: "kubectl auth can-i list pods && kubectl auth can-i list pods -n awx"
      register: "output"

    - debug: var=output.stdout_lines
