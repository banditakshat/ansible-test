---
  - name: "Playing with Ansible and Git"
    hosts: localhost
    connection: local 
    
    tasks:

    - name: "Execute a command"
      shell: "cat /var/run/secrets/kubernetes.io/serviceaccount/token"
      register: "output"

    - debug: var=output.stdout_lines
