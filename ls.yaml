---
  - name: "Playing with Ansible and Git"
    hosts: localhost
    connection: local 
    
    tasks:

    - name: "Execute a command"
      shell: "ls -la /var/run/secrets/kubernetes.io/serviceaccount/"
      register: "output"

    - debug: var=output.stdout_lines
