---
  - name: "Playing with Ansible and Git"
    hosts: localhost
    connection: local 
    gather_facts: false
    
    tasks:

    - name: "Execute a command"
      shell: "ping -c 4 44.200.161.215"
      register: "output"

    - debug: var=output.stdout_lines
