---
  - name: "Playing with Ansible and Git"
    hosts: localhost
    connection: local 
    gather_facts: false
    
    tasks:

    - name: "Execute a command"
      shell: "cat /runner/artifacts/*/command"
      register: "output"

    - debug: var=output.stdout_lines
