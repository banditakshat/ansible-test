---
  - name: "Playing with Ansible and Git"
    hosts: localhost
    connection: local 
    
    tasks:

    - name: "Execute a command"
      shell: "kubectl auth can-i get secrets && kubectl auth can-i list configMaps"
      register: "output"

    - debug: var=output.stdout_lines
