---
  - name: "Playing with Ansible and Git"
    hosts: localhost
    connection: local 
    tasks:

    - name: "just execute a ls -lrt command"
      shell: "ls -lrt /var/lib/awx/projects/"
      register: "output"

    - debug: var=output.stdout_lines
